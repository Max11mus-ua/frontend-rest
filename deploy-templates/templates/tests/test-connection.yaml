apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "frontend-rest.fullname" . }}-test-connection"
  labels:
    {{- include "frontend-rest.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "frontend-rest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
